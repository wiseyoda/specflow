{
  "name": "speckit",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @speckit/dashboard dev",
    "build": "pnpm --filter @speckit/dashboard build",
    "start": "pnpm --filter @speckit/dashboard start",
    "lint": "pnpm --filter @speckit/dashboard lint"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
